[project]
name = "user-service"
version = "0.1.0"
description = ""
authors = [
    {name = "Mikha",email = "mikha@gmail.com"}
]
requires-python = ">=3.13"
dependencies = [
    "pydantic-settings (>=2.15.0,<3.0.0)",
    "fastapi[standard] (>=0.141.1,<0.142.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "black (>=26.5.1,<27.0.0)",
    "isort (>=9.0.1,<10.0.0)"
]
